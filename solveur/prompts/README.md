# Prompts versionnés

Chaque prompt template vit dans un dossier `<id>_<version>/` (ex. `direct_v1/`)
avec son texte et ses métadonnées. Un prompt utilisé dans une étude
comparative est **gelé** : toute modification passe par une nouvelle version.
